# ==============================================================
# robots.txt — Harbour Island Insider
# https://harbourislandinsider.com
# Last updated: 2026-04-19
# ==============================================================


# --------------------------------------------------------------
# Default rules for all crawlers
# --------------------------------------------------------------
User-agent: *
Allow: /

# WordPress admin and system paths — never crawl these
Disallow: /wp-admin/
Disallow: /wp-includes/
Disallow: /wp-content/plugins/

# Search result pages — duplicate content, no SEO value
Disallow: /?s=
Disallow: /search/

# Tag archives — thin content, prefer category/journal archive
Disallow: /tag/

# Feed URLs — not useful for search engines
Disallow: /feed/
Disallow: /comments/feed/

# WP login and registration — security
Disallow: /wp-login.php
Disallow: /wp-register.php

# XML-RPC — unnecessary and a common attack vector
Disallow: /xmlrpc.php

# Author archive pages — duplicate content risk
Disallow: /author/

# Pagination of tag/author pages (duplicate content)
Disallow: /tag/*/page/
Disallow: /author/*/page/


# --------------------------------------------------------------
# Googlebot — allow CSS and JS for full rendering
# Google needs to render JavaScript and stylesheets to evaluate
# page quality, so we explicitly allow these even though
# wp-includes and wp-content/plugins are otherwise blocked.
# --------------------------------------------------------------
User-agent: Googlebot
Allow: /wp-content/themes/
Allow: /wp-content/uploads/
Allow: /*.js$
Allow: /*.css$
Allow: /*.png$
Allow: /*.jpg$
Allow: /*.jpeg$
Allow: /*.gif$
Allow: /*.webp$
Allow: /*.svg$
Disallow: /wp-admin/
Disallow: /wp-includes/
Disallow: /wp-content/plugins/
Disallow: /?s=
Disallow: /search/
Disallow: /tag/


# --------------------------------------------------------------
# Googlebot-Image — allow all uploaded images
# --------------------------------------------------------------
User-agent: Googlebot-Image
Allow: /wp-content/uploads/


# --------------------------------------------------------------
# AI crawlers — DISALLOWED
#
# We disallow AI training crawlers because this blog's content
# is original editorial work. We do not consent to our articles,
# photography captions, or itinerary copy being used as training
# data for large language models. This does NOT affect Google or
# Bing's standard search indexing bots above.
# --------------------------------------------------------------

# OpenAI / ChatGPT
User-agent: GPTBot
Disallow: /

# OpenAI browsing plugin
User-agent: ChatGPT-User
Disallow: /

# Common Crawl (used by many LLM training datasets)
User-agent: CCBot
Disallow: /

# Anthropic Claude (web crawl for training)
User-agent: anthropic-ai
Disallow: /

# Google Extended (Bard / Gemini training)
User-agent: Google-Extended
Disallow: /

# Meta / Facebook AI training crawler
User-agent: FacebookBot
Disallow: /

# Cohere AI
User-agent: cohere-ai
Disallow: /

# PerplexityBot (AI answer engine — disallow to protect traffic)
User-agent: PerplexityBot
Disallow: /

# Bytedance / TikTok AI
User-agent: Bytespider
Disallow: /

# Apple Applebot-Extended (used for Apple Intelligence training)
User-agent: Applebot-Extended
Disallow: /


# --------------------------------------------------------------
# Sitemap location
# --------------------------------------------------------------
Sitemap: https://harbourislandinsider.com/sitemap.xml
